<Page x:Class="WarehousePOS.Desktop.Views.Products.ProductListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Products"
      Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header row -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Products" Style="{StaticResource H2}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="+ Add Product"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding AddCommand}"/>
        </Grid>

        <!-- Filter bar -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16" Padding="16,12">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="280"
                         Style="{StaticResource FormTextBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,12,0">
                    <TextBox.ToolTip>Search by name, SKU or barcode</TextBox.ToolTip>
                </TextBox>

                <ComboBox Width="200"
                          Margin="0,0,12,0"
                          SelectedValuePath="Id"
                          DisplayMemberPath="Name"
                          SelectedValue="{Binding FilterCategoryId}"
                          ItemsSource="{Binding Categories}">
                    <ComboBox.ToolTip>Filter by category</ComboBox.ToolTip>
                </ComboBox>

                <CheckBox Content="Show Inactive"
                          IsChecked="{Binding ShowInactive}"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Product grid -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="SKU"             Binding="{Binding SKU}"           Width="120"/>
                    <DataGridTextColumn Header="Name"            Binding="{Binding Name}"           Width="*"/>
                    <DataGridTextColumn Header="Category"        Binding="{Binding CategoryName}"   Width="130"/>
                    <DataGridTextColumn Header="Retail (Rs.)"    Binding="{Binding RetailPrice, StringFormat=N2}"    Width="110"/>
                    <DataGridTextColumn Header="Wholesale (Rs.)" Binding="{Binding WholesalePrice, StringFormat=N2}" Width="120"/>
                    <DataGridTextColumn Header="Stock"           Binding="{Binding StockQuantity}"  Width="70"/>
                    <DataGridTextColumn Header="Status"          Binding="{Binding IsActive, Converter={StaticResource BoolToStatusConverter}}" Width="80"/>
                    <DataGridTemplateColumn Header="Actions"     Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="8,4" Margin="0,0,6,0"
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="{Binding IsActive, Converter={StaticResource BoolToToggleTextConverter}}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="8,4"
                                            Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>

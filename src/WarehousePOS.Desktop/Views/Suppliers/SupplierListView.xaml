<Page x:Class="WarehousePOS.Desktop.Views.Suppliers.SupplierListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Suppliers"
      Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Suppliers" Style="{StaticResource H2}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="+ Add Supplier"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding AddCommand}"/>
        </Grid>

        <!-- Filters -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16" Padding="16,12">
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="Show Inactive" IsChecked="{Binding ShowInactive}" VerticalAlignment="Center"/>
                <Button Content="Refresh" Style="{StaticResource SecondaryButton}"
                        Margin="16,0,0,0" Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Border>

        <!-- Grid -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"           Binding="{Binding Name}"          Width="*"/>
                    <DataGridTextColumn Header="Contact"        Binding="{Binding ContactPerson}"  Width="140"/>
                    <DataGridTextColumn Header="Phone"          Binding="{Binding Phone}"          Width="120"/>
                    <DataGridTextColumn Header="Balance (Rs.)"  Binding="{Binding Balance, StringFormat=N2}" Width="120"/>
                    <DataGridTextColumn Header="Status"         Binding="{Binding IsActive, Converter={StaticResource BoolToStatusConverter}}" Width="80"/>
                    <DataGridTemplateColumn Header="Actions"    Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit"
                                            Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,6,0"
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="{Binding IsActive, Converter={StaticResource BoolToToggleTextConverter}}"
                                            Style="{StaticResource SecondaryButton}" Padding="8,4"
                                            Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
